<%= form_with(model: iventory_product) do |form| %>
  <% if iventory_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(iventory_product.errors.count, "error") %> prohibited this iventory_product from being saved:</h2>

      <ul>
        <% iventory_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
